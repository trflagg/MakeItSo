import React from 'react';

const DMScreen = ({ screenHeight}) => (
  <div id="dmScreen" style={{
    minHeight: screenHeight,
    background: '#ddd',
  }}>
    Hello Wrold
  </div>
);

export default DMScreen;
