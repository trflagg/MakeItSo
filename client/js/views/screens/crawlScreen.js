var dot = require('dot')
, SimpleScreen = require('./simpleScreen')

// crawlScreen is simpleScreen but without the header
// see gameMode.renderScreen()
module.exports = crawlScreen = SimpleScreen.extend({

});
